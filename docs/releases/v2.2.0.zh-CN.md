# v2.2.0 发布说明

语言版本：中文 | [English](v2.2.0.md)

发布日期：2026-07-08

## 摘要

`v2.2.0` 将项目整理为更适合开源审核和使用的形态：补齐中英文入口文档、公开演示素材、示例 SQL、示例输出，以及贡献、安全、路线图等维护文档。

## 30 秒演示

![Lineage demo GIF](../assets/demo-lineage-flow.gif)

![Lineage graph screenshot](../assets/demo-lineage-graph.png)

演示展示核心流程：

1. 输入目标表和目标字段。
2. 查询上游/下游血缘。
3. 查看带分层、路径、边和节点元数据的血缘图谱。
4. 将查询结果作为影响分析、审计复核和数据治理证据。

## 安装

```bash
git clone git@github.com:NickyLam/DATA_RELY_ANALYSIS_PLATFORM.git
cd DATA_RELY_ANALYSIS_PLATFORM

python3.11 -m venv .venv
source .venv/bin/activate
python3.11 -m pip install -r requirements.txt

python3.11 run_app.py
```

打开：

- 前端：`http://localhost:8899/static/index.html`
- API 文档：`http://localhost:8899/docs`

强制重新解析数据源：

```bash
python3.11 run_app.py --reparse
```

## 示例 SQL

```sql
CREATE TABLE ICL.CUSTOMER_DAILY_SNAPSHOT AS
SELECT
    c.CUST_ID,
    c.CUST_NAME,
    a.ACCT_BALANCE,
    CASE WHEN a.ACCT_BALANCE > 100000 THEN 'VIP' ELSE 'STANDARD' END AS CUSTOMER_TIER
FROM IML.CUSTOMER_PROFILE c
LEFT JOIN IOL.ACCOUNT_BALANCE a
    ON c.CUST_ID = a.CUST_ID;
```

完整示例：[`docs/examples/oracle_warehouse_lineage.sql`](../examples/oracle_warehouse_lineage.sql)

## 查询示例

```bash
curl -X POST http://localhost:8899/api/lineage/query \
  -H 'Content-Type: application/json' \
  -d '{
    "target_table": "ICL.CUSTOMER_DAILY_SNAPSHOT",
    "target_field": "CUSTOMER_TIER",
    "direction": "upstream"
  }'
```

## 输出示例

```json
{
  "target": {
    "table": "ICL.CUSTOMER_DAILY_SNAPSHOT",
    "field": "CUSTOMER_TIER"
  },
  "direction": "upstream",
  "summary": {
    "node_count": 4,
    "edge_count": 3,
    "max_depth": 2
  },
  "nodes": [
    {
      "id": "ICL.CUSTOMER_DAILY_SNAPSHOT.CUSTOMER_TIER",
      "table": "ICL.CUSTOMER_DAILY_SNAPSHOT",
      "field": "CUSTOMER_TIER",
      "layer": "ICL"
    },
    {
      "id": "IOL.ACCOUNT_BALANCE.ACCT_BALANCE",
      "table": "IOL.ACCOUNT_BALANCE",
      "field": "ACCT_BALANCE",
      "layer": "IOL"
    }
  ],
  "edges": [
    {
      "source": "IOL.ACCOUNT_BALANCE.ACCT_BALANCE",
      "target": "ICL.CUSTOMER_DAILY_SNAPSHOT.CUSTOMER_TIER",
      "transform": "CASE WHEN ACCT_BALANCE > 100000 THEN 'VIP' ELSE 'STANDARD' END"
    }
  ]
}
```

机器可读样例：[`docs/examples/lineage_query_output.json`](../examples/lineage_query_output.json)

## 变更摘要

### 新增

- README 和相关维护文档整理为中文/英文两个版本。
- 发布说明包含安装命令、示例 SQL、输出示例、截图和 GIF。
- 增加 `CONTRIBUTING`、`SECURITY`、`ROADMAP` 和 GitHub Issue 模板。
- 在 `docs/assets/` 增加公开演示素材。

### 修复和强化

- `_ex` 交换表工作流中的字段血缘保持完整。
- 数仓层级检测覆盖 SRC、MSL、ITL、IOL、ICL、IML、IDL、IEL、DQC 等企业数仓层。
- 前端图谱展示分层颜色、路径和节点详情，便于快速复核。

## 发布前验证建议

```bash
python3.11 -m pytest tests/
ruff check app/ core/ tests/
mypy app/ core/
```
