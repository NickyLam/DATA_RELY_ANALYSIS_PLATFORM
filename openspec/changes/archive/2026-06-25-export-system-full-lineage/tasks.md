## 1. Workbook Contract

- [x] 1.1 Define workbook-ready row contracts for `Summary`, `Tables`, `TableLineages`, and `FieldMappings` using Python 3.11-compatible typing.
- [x] 1.2 Add an XLSX writer utility or service using the existing `openpyxl` dependency to generate workbook bytes from those row contracts.
- [x] 1.3 Ensure workbook sheet names and header columns match the `system-full-lineage-export` spec exactly.

## 2. System Classification

- [x] 2.1 Extract or share system membership helpers so export filtering and `TableQueryService.get_tables_by_system()` use consistent `data_source`-first and schema-fallback rules.
- [x] 2.2 Add focused unit coverage for explicit `data_source` precedence and schema fallback classification.

## 3. Export Assembly

- [x] 3.1 Add `LineageService.export_system_full_lineage(system_name: str) -> dict[str, Any]` that reads current parser data without triggering parsing or cache rebuild and returns workbook-ready data.
- [x] 3.2 Include selected-system table nodes plus external referenced nodes for included table lineage edges and field mappings.
- [x] 3.3 Include every table-level edge and field mapping where either endpoint belongs to the requested system, preserving procedure and transformation metadata when present.
- [x] 3.4 Include deterministic metadata and counts: `system_name`, `generated_at`, available data timestamps/generation, total nodes, system nodes, external nodes, table lineage edges, and field mappings.
- [x] 3.5 Return service-level not-found/no-data signals that the API layer can map to user-safe HTTP 404 errors.

## 4. API Endpoint

- [x] 4.1 Add `GET /api/systems/{system_name}/lineage/export` in `app/api/lineage.py` returning an XLSX download with the correct media type and `Content-Disposition` filename.
- [x] 4.2 Map unknown systems and unavailable parsed data to HTTP 404 with user-safe messages.
- [x] 4.3 Document the binary XLSX 200 response in FastAPI route metadata/OpenAPI.

## 5. Verification

- [x] 5.1 Add service tests for one-system export, cross-system inbound/outbound edges, external node marking, field mappings, and no-data behavior.
- [x] 5.2 Add XLSX writer tests that load generated bytes with `openpyxl` and assert required worksheet names, headers, metadata, and representative rows.
- [x] 5.3 Add API tests for successful XLSX download headers/content, unknown system 404, and OpenAPI binary response documentation.
- [x] 5.4 Run focused verification: `python3.11 -m pytest tests/test_lineage_api.py tests/test_source_data_report_fixes.py -v`.
- [x] 5.5 Run full verification before completion: `python3.11 -m pytest tests/`.
