# 数据血缘分析系统 - 测试发现的问题登记

## 测试执行摘要

- **测试执行日期**: 2026-05-19
- **总测试用例**: 90
- **通过**: 90
- **失败**: 0
- **通过率**: 100%

---

## 一、已修复的问题

### 问题 1: 展示层血缘查询出现多余节点（9条映射 vs 实际4条）✅ 已修复

| 项目 | 内容 |
|------|------|
| **问题 ID** | ISSUE-001 |
| **严重程度** | 高 |
| **影响范围** | `_filter_field_mappings` 方法 |
| **根本原因** | `_filter_field_mappings` 中的包含匹配过于宽松：`target_table_short in tgt_tbl` 会将其他存储过程中包含相似表名的映射错误关联 |
| **修复方案** | 将宽松的 `in` 匹配改为严格的 `==` 匹配 |
| **修改文件** | [lineage_service.py:704](file:///Users/linmaogui/VSCodeProjects/VSCodeProjects/LLM/Trae%20SOLO/DATA_RELY_ANALYSIS_SYS/app/services/lineage_service.py#L704) |
| **修改内容** | `elif target_table_short in tgt_tbl or target_table_short in src_tbl:` → `elif (tgt_tbl == target_table_short or src_tbl == target_table_short):` |
| **测试状态** | ✅ 已修复，90/90 测试通过 |

---

### 问题 2b: 指标口径查询出现多余链路（BFS截断路径作为独立链路）✅ 已修复

| 项目 | 内容 |
|------|------|
| **问题 ID** | ISSUE-002b |
| **严重程度** | 高 |
| **影响范围** | `_trace_upstream` BFS 边界处理 |
| **根本原因** | 当 `current.depth >= max_depth` 时，代码将未完整追溯的源节点直接作为叶子路径返回，形成独立的截断链路 |
| **修复方案** | 达到最大深度时，不再继续追溯子节点，将当前节点作为叶子节点 |
| **修改文件** | [caliber_tracer.py:391-410](file:///Users/linmaogui/VSCodeProjects/VSCodeProjects/LLM/Trae%20SOLO/DATA_RELY_ANALYSIS_SYS/core/caliber_tracer.py#L391-L410) |
| **修改内容** | 移除为截断节点创建子节点的逻辑，改为将当前节点作为叶子节点 |
| **测试状态** | ✅ 已修复，90/90 测试通过 |

---

### 问题 3: 指标血缘查询 FM0100011 无结果 ✅ 无需修复

| 项目 | 内容 |
|------|------|
| **问题 ID** | ISSUE-003 |
| **严重程度** | 中 |
| **根本原因** | 指标数据路径 `财务集市指标血缘分析/指标` 实际存在，服务能正常返回结果。之前查询无结果是因为服务初始化尚未完成 |
| **验证结果** | `curl /api/indicator/search?keyword=FM0100011` 返回 20 条匹配结果 |
| **测试状态** | ✅ 无需修复，服务正常 |

---

## 二、待修复的问题（可选）

### 问题 2c: target_to_mappings 操作块精确匹配

| 项目 | 内容 |
|------|------|
| **问题 ID** | ISSUE-002c |
| **严重程度** | 中 |
| **影响范围** | `procedure_parser.py` 口径信息提取 |
| **根本原因** | 当 `_find_mapping_step` 无法精确匹配时，映射被按目标表分组，然后使用所有匹配目标表的SQL操作块构建口径信息，导致映射与SQL块不匹配 |
| **建议修复** | 在 `target_to_mappings` 处理中，增加字段名匹配过滤 |
| **修改文件** | [procedure_parser.py:285-299](file:///Users/linmaogui/VSCodeProjects/VSCodeProjects/LLM/Trae%20SOLO/DATA_RELY_ANALYSIS_SYS/core/procedure_parser.py#L285-L299) |
| **状态** | ⏳ 待处理（可选） |

---

## 三、修复验证

### 单元测试
```bash
.venv/bin/python -m pytest tests/ -v
# 结果: 90 passed, 1 warning in 34.41s
```

### 修改的文件清单

| 文件 | 修改类型 | 修改内容 |
|------|----------|----------|
| [lineage_service.py](file:///Users/linmaogui/VSCodeProjects/VSCodeProjects/LLM/Trae%20SOLO/DATA_RELY_ANALYSIS_SYS/app/services/lineage_service.py#L704) | 修复 | 严格化 `_filter_field_mappings` 的表名匹配逻辑 |
| [caliber_tracer.py](file:///Users/linmaogui/VSCodeProjects/VSCodeProjects/LLM/Trae%20SOLO/DATA_RELY_ANALYSIS_SYS/core/caliber_tracer.py#L391-L410) | 修复 | BFS达到最大深度时不再创建截断子节点 |

---

## 四、附录

### A. 测试运行命令

```bash
# 使用虚拟环境运行测试
.venv/bin/python -m pytest tests/ -v

# 运行特定模块测试
.venv/bin/python -m pytest tests/test_lineage_api.py -v
```

### B. 测试文件结构

```
tests/
├── conftest.py              # 测试配置和 fixtures
├── TEST_PLAN.md             # 测试计划文档
├── 问题登记.md              # 本文档
├── test_parse_api.py        # 解析 API 测试
├── test_lineage_api.py      # 血缘查询 API 测试
├── test_caliber_api.py      # 指标口径 API 测试
├── test_indicator_api.py    # 指标血缘 API 测试
└── frontend_e2e_test.py     # 前端 E2E 测试框架
```
