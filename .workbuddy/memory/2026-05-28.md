# 2026-05-28 工作日志

## 项目整体代码质量评估
- 完成了对 DATA_RELY_ANALYSIS_SYS 项目的全面软件工程评估
- 产出报告：`docs/CODE_QUALITY_ASSESSMENT.md`
- 综合评分：6.32/10（C+，中等偏上）
- 核心发现：
  - 架构顶层设计优秀（Protocol/Registry/EventBus/DI），但核心引擎实现粗糙
  - 最大问题：procedure_parser.py 与 table_name_resolver.py 代码分叉（~75行重复）
  - 5个 God Method（_bfs_trace 160行、extract_caliber_from_proc 117行、export_markdown 219行等）
  - 代码重复总计 ~395 行（占 core/ 的 8-10%）
  - Pydantic 响应模型定义了但未在路由中使用，OpenAPI 文档不准确
  - 无 API 版本控制，测试覆盖率约 18.6%
- P0 优先修复：消除 procedure_parser.py 对 TableNameResolver 的重复实现

## P0~P3 改造详细设计方案
- 产出文档：`docs/REFACTOR_DESIGN.md`
- 覆盖 20 个改造项，从 P0（立即修复）到 P3（长期演进）
- 每项均包含：问题定位（行号级）、代码级改造方案（含完整代码示例）、验证步骤、预期收益
- P0 核心：TableNameResolver 委托消除（20个调用点逐一列出）、Pydantic 模型接入路由、业务逻辑下沉、私有属性访问消除
- P1 核心：God Method 拆分、BFS 方向参数化（合并~100行重复）、枚举合并、统一错误处理、pytest-cov、API v1 版本控制
- P2 核心：LineageTracer SRP 拆分为 4 类、CaliberExtractor 拆分、公共括号匹配工具、warehouse 测试、Mock 数据层
- P3 核心：解析器直接实现 Protocol、JWT 认证、Vite+TS 前端工程化、异步 BFS
- 附加：改造检查清单、风险与回滚策略、新增文件清单

## OpenSpec 提案：级联查询向导（系统→用户→表→字段）
- 创建变更提案：`spec/changes/add-cascading-query-wizard/`
- 提案目标：将展示层查询从自由搜索改为级联选择向导
- 新增 3 个后端 API：systems 列表、schemas 列表、按 schema 过滤的表列表
- 前端新增级联选择器 + 保留高级搜索入口
- 可行性评估：✅ 高度可行，预估 3-5 个工作日
- 关键约束："用户"概念需统一为 schema/namespace；跨系统血缘不受限制；高级用户保留快速搜索

## 级联查询向导实施完成
- 变更 ID: `add-cascading-query-wizard`
- 后端新增 3 个 API 端点：`GET /api/v1/systems`, `GET /api/v1/systems/{system}/schemas`, `GET /api/v1/systems/{system}/schemas/{schema}/tables`
- 后端新增 `_build_schema_to_system()` 私有方法（每次调用重新计算，无共享状态）
- 后端新增 `get_systems()`, `get_schemas()`, `get_tables_by_schema()` 到 `TableQueryService` + `LineageService` 委托
- 前端新增 `cascading-wizard.js` 模块（4级级联选择器 + 模式切换）
- 前端修改 `index.html`：级联向导为默认面板，高级搜索为折叠面板
- 前端新增 CSS 样式：`.query-mode-switcher`, `.cascade-select`, `.cascading-row` 等
- 测试结果：后端 7 项测试全部通过（系统列表 5 个系统、RRP 9 个 schema、RRP_MDL 2908 表、EAST5 过滤 204 结果、ICL 142 表、不存在系统返回空、__unclassified__ 正确）
- Code Review 完成，修复 2 个 P0（XSS 转义 + 共享可变状态）、1 个 P1（keyword 参数长度限制）、2 个 P2（初始化重试机制 + schema 验证清理）
- 9 个 schema 全部自动发现（RRP_MDL 2908, IOL 4423, IML 1192, IDL 823, ITL 539, MSL 163, ICL 142, RRP_EAST 133, UNKNOWN 1）

## OpenSpec 归档：add-cascading-query-wizard
- 变更已归档到 `spec/archive/2026-05-28-add-cascading-query-wizard/`
- Living spec 已创建：`spec/specs/cascading-query/spec.md`（6 个 Requirement + 13 个 Scenario）
- changes 目录已清空，无活跃变更
