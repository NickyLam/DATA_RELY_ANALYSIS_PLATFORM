# 2026-05-20 工作日志

## 重新解析所有数据源（含数仓脚本）

### 背景
- 用户发现系统只包含 Oracle 数据（3041表 + 1754过程）
- 缺少数仓脚本数据（21455 .sql + 179 .ctl）
- 数仓脚本解析器已实现但未激活

### 问题分析
- 缓存文件 `lineage_data.pkl` 只包含 Oracle 数据
- 服务启动时优先加载缓存，跳过了数仓脚本解析
- 需删除缓存触发全量解析

### 执行操作
1. 删除旧缓存文件：
   - `/output/lineage_data.pkl`（1.9G）
   - `/output/lineage_data.json`（2.5G）

2. 停止旧服务进程（PID 13893）

3. 重启服务触发全量解析（PID 44837）：
   - 数仓脚本解析器已注册（WarehouseSQLParser）
   - 解析流程：Oracle → 数仓脚本 → 管架脚本

4. 监控进度：
   - 日志文件：`/tmp/uvicorn_reparse.log`
   - 当前阶段：Oracle RRP_MDL 口径提取（15:01）
   - CPU占用：93.1%

### 解析器注册状态
- OracleTabAdapter: `.tab`（表结构）
- OraclePrcAdapter: `.prc`（存储过程）
- WarehouseSQLParser: `.sql/.ctl`（数仓脚本）

### 预计结果
- Oracle: 3041 表 + 1754 过程 + 27529 血缘
- 数仓脚本: ~618 表 + ~263 过程 + ~2200 血缘（管架）
- 总计: ~3659 表 + ~2017 过程 + ~29700 血缘

### 预计完成时间
- 15:20-15:25（约20分钟）

### 监控命令
```bash
tail -f /tmp/uvicorn_reparse.log
```

### API验证
解析完成后访问：
```bash
curl http://localhost:8899/api/system/stats
```

---
_记录时间：2026-05-20 15:02_