# 2026-05-14 工作日志

## 修复：ICL 多余节点 + 字段映射重复 + 同表内映射误过滤

### 背景
上一会话已修复了 `EAST5_201_GRJCXXB.KHXM` 查询的两个 Bug：
1. 血缘图中多了 `ICL.CMM_INDV_CUST_BASIC_INFO` 节点（应与 `O_ICL_CMM_INDV_CUST_BASIC_INFO` 合并）
2. 字段映射数据重复

但修复中引入了一个新问题：自环边过滤 `src_bare == tgt_bare` 过于激进，误伤了同表内不同字段间的转换边（如 `CUST_NM → CUST_NM_DESEN` 脱敏映射）。

### 本会话修复
1. **重启服务验证上一会话修复** — 确认 ICL 节点合并和字段映射去重已生效
2. **发现同表内映射被误过滤** — `M_CUST_IND_INFO_EAST.CUST_NM → CUST_NM_DESEN` 缺失，原因是 `src_bare == tgt_bare` 过滤条件过于宽泛
3. **修改自环边过滤条件**（`api_server.py` 第 314-316 行）：
   - 旧：`if src_bare == tgt_bare: continue`
   - 新：`if src_bare == tgt_bare and src_table != tgt_table: continue`
   - 原理：schema 变体假边的原始表名不同（如 `ICL.V_*` vs `RRP_MDL.O_ICL_*`），而同表内字段映射的原始表名相同
4. **重启服务验证** — 最终结果正确：4 节点、4 边、4 映射

### 验证结果
查询 `RRP_MDL.EAST5_201_GRJCXXB.KHXM` 完整血缘链路：
```
O_ICL_CMM_INDV_CUST_BASIC_INFO.CUST_NAME → M_CUST_IND_INFO.CUST_NM → M_CUST_IND_INFO_EAST.CUST_NM → M_CUST_IND_INFO_EAST.CUST_NM_DESEN → EAST5_201_GRJCXXB.KHXM
```

### 注意
- 服务初始化耗时约 6-7 分钟（Python 3.9.23, M1 Mac）
- 修改 `api_server.py` 后必须重启服务才能生效
